<!DOCTYPE html>
<html>
  <head>
    <title>RailsAppTheZoo</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <!-- the CONTAINER around the NAVBAR and FLASHES has been added by me based on AIRBNB Inspiration -->
    <!-- the default NAVBAR and FLASHED are usually inside the body -->
    <div class="container">
      <%= render 'shared/navbar' %>
      <%= render 'shared/flashes' %>
    </div>

    <%= yield %>
  </body>
</html>
